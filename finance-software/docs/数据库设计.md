# 小微企业财务管理系统 - 数据库设计文档

## 文档信息

| 项目 | 内容 |
|------|------|
| 项目名称 | 小微企业财务管理系统 |
| 版本 | v1.0 |
| 状态 | 正式版 |
| 作者 | 萍子AI团队 |
| 创建日期 | 2026-03-27 |
| 最后更新 | 2026-03-27 |

---

## 1. 数据库架构设计

### 1.1 多账套架构模式

系统采用类似用友的多账套架构：**公共数据库 + 独立账套数据库**

```
┌─────────────────────────────────────────┐
│              应用服务层                  │
│         (NestJS / 路由分发)             │
└────────────────┬────────────────────────┘
                 │
      ┌──────────┴──────────┐
      │                     │
      ▼                     ▼
┌─────────────┐    ┌─────────────────────┐
│ 公共数据库   │    │    账套数据库        │
│ (finance_pub)│    │  (finance_001)     │
├─────────────┤    ├─────────────────────┤
│ • 用户表    │    │ • 科目表            │
│ • 角色表    │    │ • 凭证表            │
│ • 用户角色  │    │ • 凭证分录表        │
│ • 账套表    │    │ • 账户表            │
│ • 系统配置  │    │ • 流水表            │
│ • 操作日志  │    │ • 发票表            │
└─────────────┘    │ • 税务记录表        │
      ▲            └─────────────────────┘
      │                     ▲
      │         ...         │
      └─────────────────────┘
                   │
            ┌────────────────┐
            │  账套数据库 N   │
            │ (finance_xxx)  │
            └────────────────┘
```

### 1.2 数据库命名规范

| 规则 | 示例 |
|------|------|
| 表名 | 单词全小写，下划线分隔，如 `user_roles` |
| 字段名 | 单词全小写，下划线分隔，如 `created_at` |
| 主键 | 使用 `id`，类型为 UUID |
| 外键 | `[表名]_id` 格式，如 `user_id` |
| 索引 | `idx_[表名]_[字段]` 格式 |

---

## 2. 公共数据库 (finance_pub)

### 2.1 表清单

| 表名 | 说明 |
|------|------|
| `users` | 系统用户（跨账套管理员） |
| `roles` | 角色定义 |
| `user_roles` | 用户角色关联 |
| `tenants` | 账套信息 |
| `sys_config` | 系统配置 |
| `audit_log` | 操作日志 |

### 2.2 表详细设计

#### 2.2.1 users - 用户表

```sql
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    username VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    real_name VARCHAR(50) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    phone VARCHAR(20),
    status VARCHAR(20) DEFAULT 'active' CHECK (status IN ('active', 'inactive', 'disabled')),
    is_super_admin BOOLEAN DEFAULT FALSE,
    tenant_id UUID,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_users_username ON users(username);
CREATE INDEX idx_users_tenant_id ON users(tenant_id);
CREATE INDEX idx_users_status ON users(status);
```

**说明**：系统用户分为两类：
- 超级管理员（is_super_admin=true）：可管理所有账套
- 普通用户（tenant_id）：属于特定账套

#### 2.2.2 roles - 角色表

```sql
CREATE TABLE roles (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    code VARCHAR(50) UNIQUE NOT NULL,
    name VARCHAR(50) NOT NULL,
    description TEXT,
    permissions JSONB DEFAULT '[]',
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_roles_code ON roles(code);
CREATE INDEX idx_roles_is_active ON roles(is_active);
```

**permissions 字段示例**：
```json
["account:read", "account:write", "voucher:read", "voucher:write", "voucher:approve"]
```

#### 2.2.3 user_roles - 用户角色关联表

```sql
CREATE TABLE user_roles (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    role_id UUID NOT NULL REFERENCES roles(id) ON DELETE CASCADE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    UNIQUE(user_id, role_id)
);

CREATE INDEX idx_user_roles_user_id ON user_roles(user_id);
CREATE INDEX idx_user_roles_role_id ON user_roles(role_id);
```

#### 2.2.4 tenants - 账套表

```sql
CREATE TABLE tenants (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    code VARCHAR(20) UNIQUE NOT NULL,
    name VARCHAR(100) NOT NULL,
    db_name VARCHAR(50) UNIQUE NOT NULL,
    status VARCHAR(20) DEFAULT 'active' CHECK (status IN ('active', 'inactive', 'expired')),
    expired_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_tenants_code ON tenants(code);
CREATE INDEX idx_tenants_status ON tenants(status);
```

#### 2.2.5 sys_config - 系统配置表

```sql
CREATE TABLE sys_config (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    config_key VARCHAR(100) UNIQUE NOT NULL,
    config_value TEXT,
    description VARCHAR(255),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_sys_config_key ON sys_config(config_key);
```

**配置项示例**：
| config_key | config_value | description |
|------------|--------------|-------------|
| tax_rate_default | 13 | 默认税率 |
| voucher_need_approve | true | 凭证是否需要审核 |
| invoice_max_file_size | 10485760 | 发票附件最大10MB |
| backup_retention_days | 30 | 备份保留天数 |

#### 2.2.6 audit_log - 操作日志表

```sql
CREATE TABLE audit_log (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id),
    tenant_id UUID,
    action VARCHAR(50) NOT NULL,
    resource_type VARCHAR(50) NOT NULL,
    resource_id VARCHAR(100),
    ip_address VARCHAR(50),
    user_agent TEXT,
    request_data JSONB,
    response_status INTEGER,
    error_message TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_audit_log_user_id ON audit_log(user_id);
CREATE INDEX idx_audit_log_tenant_id ON audit_log(tenant_id);
CREATE INDEX idx_audit_log_action ON audit_log(action);
CREATE INDEX idx_audit_log_created_at ON audit_log(created_at);
```

---

## 3. 账套数据库

### 3.1 表清单

| 分类 | 表名 | 说明 |
|------|------|------|
| 基础 | `accounts` | 会计科目 |
| | `subjects` | 科目辅助核算项 |
| 账务 | `vouchers` | 凭证主表 |
| | `voucher_items` | 凭证分录 |
| | `voucher_templates` | 凭证模板 |
| 资金 | `bank_accounts` | 银行账户 |
| | `cash_flows` | 资金流水 |
| 发票 | `invoices` | 发票池 |
| 税务 | `tax_records` | 税务记录 |

### 3.2 表详细设计

#### 3.2.1 accounts - 会计科目表

```sql
CREATE TABLE accounts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    code VARCHAR(20) NOT NULL,
    name VARCHAR(100) NOT NULL,
    type VARCHAR(20) NOT NULL CHECK (type IN ('asset', 'liability', 'equity', 'revenue', 'cost', 'expense')),
    level INTEGER DEFAULT 1 CHECK (level BETWEEN 1 AND 5),
    parent_id UUID REFERENCES accounts(id),
    is_detail BOOLEAN DEFAULT TRUE,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    UNIQUE(code)
);

CREATE INDEX idx_accounts_code ON accounts(code);
CREATE INDEX idx_accounts_type ON accounts(type);
CREATE INDEX idx_accounts_parent_id ON accounts(parent_id);
CREATE INDEX idx_accounts_is_active ON accounts(is_active);
```

**科目类型说明**：
| type | 说明 | 示例科目 |
|------|------|----------|
| asset | 资产 | 1001银行存款、1002库存现金 |
| liability | 负债 | 2001短期借款、2202应付账款 |
| equity | 所有者权益 | 4001实收资本、4103利润分配 |
| revenue | 收入 | 5001主营业务收入 |
| cost | 成本 | 6001主营业务成本 |
| expense | 费用 | 6601销售费用、6602管理费用 |

#### 3.2.2 bank_accounts - 银行账户表

```sql
CREATE TABLE bank_accounts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    code VARCHAR(50) UNIQUE NOT NULL,
    name VARCHAR(100) NOT NULL,
    type VARCHAR(20) NOT NULL CHECK (type IN ('bank', 'cash', 'wechat', 'alipay')),
    balance DECIMAL(15, 2) DEFAULT 0,
    currency VARCHAR(10) DEFAULT 'CNY',
    bank_name VARCHAR(200),
    account_no VARCHAR(50),
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_bank_accounts_code ON bank_accounts(code);
CREATE INDEX idx_bank_accounts_type ON bank_accounts(type);
CREATE INDEX idx_bank_accounts_is_active ON bank_accounts(is_active);
```

#### 3.2.3 cash_flows - 资金流水表

```sql
CREATE TABLE cash_flows (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    account_id UUID NOT NULL REFERENCES bank_accounts(id),
    type VARCHAR(20) NOT NULL CHECK (type IN ('income', 'expense', 'transfer')),
    amount DECIMAL(15, 2) NOT NULL,
    date DATE NOT NULL,
    counterparty VARCHAR(200),
    remark TEXT,
    voucher_id UUID,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_cash_flows_account_id ON cash_flows(account_id);
CREATE INDEX idx_cash_flows_type ON cash_flows(type);
CREATE INDEX idx_cash_flows_date ON cash_flows(date);
CREATE INDEX idx_cash_flows_voucher_id ON cash_flows(voucher_id);
```

#### 3.2.4 vouchers - 凭证主表

```sql
CREATE TABLE vouchers (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    voucher_no VARCHAR(50) UNIQUE NOT NULL,
    voucher_date DATE NOT NULL,
    status VARCHAR(20) DEFAULT 'draft' CHECK (status IN ('draft', 'approved', 'posted')),
    attach_count INTEGER DEFAULT 0,
    attachment TEXT,
    template_type VARCHAR(50),
    maker_id UUID,
    maker_name VARCHAR(50),
    checker_id UUID,
    poster_id UUID,
    remark TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_vouchers_voucher_no ON vouchers(voucher_no);
CREATE INDEX idx_vouchers_voucher_date ON vouchers(voucher_date);
CREATE INDEX idx_vouchers_status ON vouchers(status);
CREATE INDEX idx_vouchers_maker_id ON vouchers(maker_id);
```

**状态说明**：
| status | 说明 | 流转 |
|--------|------|------|
| draft | 草稿 | 初始状态 |
| approved | 已审核 | 需要审核 |
| posted | 已过账 | 最终状态 |

#### 3.2.5 voucher_items - 凭证分录表

```sql
CREATE TABLE voucher_items (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    voucher_id UUID NOT NULL REFERENCES vouchers(id) ON DELETE CASCADE,
    account_code VARCHAR(20) NOT NULL,
    account_name VARCHAR(100) NOT NULL,
    direction VARCHAR(10) NOT NULL CHECK (direction IN ('debit', 'credit')),
    amount DECIMAL(15, 2) NOT NULL,
    description VARCHAR(200),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_voucher_items_voucher_id ON voucher_items(voucher_id);
CREATE INDEX idx_voucher_items_account_code ON voucher_items(account_code);
CREATE INDEX idx_voucher_items_direction ON voucher_items(direction);
```

#### 3.2.6 voucher_templates - 凭证模板表

```sql
CREATE TABLE voucher_templates (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    code VARCHAR(50) UNIQUE NOT NULL,
    name VARCHAR(100) NOT NULL,
    category VARCHAR(20) NOT NULL,
    description TEXT,
    items JSONB NOT NULL,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_voucher_templates_code ON voucher_templates(code);
CREATE INDEX idx_voucher_templates_category ON voucher_templates(category);
CREATE INDEX idx_voucher_templates_is_active ON voucher_templates(is_active);
```

**items 字段结构**：
```json
[
  {"accountCode": "6602", "accountName": "差旅费", "direction": "debit"},
  {"accountCode": "1002", "accountName": "库存现金", "direction": "credit"}
]
```

#### 3.2.7 invoices - 发票表

```sql
CREATE TABLE invoices (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    invoice_no VARCHAR(50) NOT NULL,
    invoice_code VARCHAR(50),
    type VARCHAR(20) NOT NULL CHECK (type IN ('VAT_SPECIAL', 'VAT_NORMAL', 'RECEIPT')),
    amount DECIMAL(15, 2) NOT NULL,
    tax_rate DECIMAL(5, 2) DEFAULT 0,
    tax_amount DECIMAL(15, 2) DEFAULT 0,
    excluding_tax_amount DECIMAL(15, 2) DEFAULT 0,
    seller_name VARCHAR(200) NOT NULL,
    seller_tax_no VARCHAR(50),
    buyer_name VARCHAR(200),
    buyer_tax_no VARCHAR(50),
    date DATE NOT NULL,
    status VARCHAR(20) DEFAULT 'pending' CHECK (status IN ('pending', 'used', 'verified')),
    attachment TEXT,
    voucher_id UUID,
    remark TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_invoices_invoice_no ON invoices(invoice_no);
CREATE INDEX idx_invoices_type ON invoices(type);
CREATE INDEX idx_invoices_seller_name ON invoices(seller_name);
CREATE INDEX idx_invoices_date ON invoices(date);
CREATE INDEX idx_invoices_status ON invoices(status);
CREATE INDEX idx_invoices_voucher_id ON invoices(voucher_id);

-- 查重索引：发票号+金额组合唯一
CREATE UNIQUE INDEX idx_invoices_unique ON invoices(invoice_no, amount) WHERE status != 'deleted';
```

#### 3.2.8 tax_records - 税务记录表

```sql
CREATE TABLE tax_records (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    type VARCHAR(20) NOT NULL CHECK (type IN ('VAT', 'INCOME_TAX', 'SURTAX')),
    period VARCHAR(20) NOT NULL CHECK (period IN ('monthly', 'quarterly', 'yearly')),
    period_start DATE NOT NULL,
    period_end DATE NOT NULL,
    sales_amount DECIMAL(15, 2) DEFAULT 0,
    sales_tax DECIMAL(15, 2) DEFAULT 0,
    purchase_amount DECIMAL(15, 2) DEFAULT 0,
    purchase_tax DECIMAL(15, 2) DEFAULT 0,
    input_tax_credit DECIMAL(15, 2) DEFAULT 0,
    tax_due DECIMAL(15, 2) DEFAULT 0,
    tax_rate DECIMAL(5, 2) DEFAULT 0,
    status VARCHAR(20) DEFAULT 'draft' CHECK (status IN ('draft', 'submitted', 'paid')),
    remark TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_tax_records_type ON tax_records(type);
CREATE INDEX idx_tax_records_period ON tax_records(period);
CREATE INDEX idx_tax_records_period_start ON tax_records(period_start);
CREATE INDEX idx_tax_records_status ON tax_records(status);
```

---

## 4. ER 图关系

### 4.1 公共数据库 ER 图

```
┌─────────────────┐       ┌─────────────────┐
│     users       │       │     roles       │
├─────────────────┤       ├─────────────────┤
│ id (PK)         │──┐    │ id (PK)         │
│ username        │  │    │ code            │
│ password        │  └───▶│ name            │
│ real_name       │       │ description     │
│ email           │       │ permissions     │
│ phone           │       │ is_active       │
│ status          │       └────────┬────────┘
│ is_super_admin  │                │
│ tenant_id (FK)  │◀───────────────┤
│ created_at      │    ┌───────────┴───────────┐
└────────┬────────┘    │                       │
         │            │                       │
         ▼            ▼                       ▼
┌─────────────────┐   ┌─────────────────┐
│    tenants      │   │   user_roles    │
├─────────────────┤   ├─────────────────┤
│ id (PK)         │   │ id (PK)         │
│ code            │   │ user_id (FK)    │
│ name            │   │ role_id (FK)    │
│ db_name         │   │ created_at      │
│ status          │   └─────────────────┘
│ expired_at      │
│ created_at      │
└─────────────────┘
```

### 4.2 账套数据库 ER 图

```
┌─────────────────────┐       ┌─────────────────────┐
│   bank_accounts    │       │      accounts       │
├─────────────────────┤       ├─────────────────────┤
│ id (PK)            │       │ id (PK)             │
│ code               │       │ code                │
│ name               │       │ name                │
│ type               │       │ type                │
│ balance            │       │ level               │
│ bank_name          │       │ parent_id (FK)      │
│ account_no         │       │ is_detail           │
│ is_active          │       │ is_active           │
└────────┬────────────┘       └─────────────────────┘
         │
         │ 1:N
         ▼
┌─────────────────────┐
│    cash_flows      │
├─────────────────────┤
│ id (PK)            │
│ account_id (FK)    │
│ type               │
│ amount             │
│ date               │
│ counterparty       │
│ voucher_id (FK)    │
└─────────────────────┘


┌─────────────────────┐       ┌─────────────────────┐
│     vouchers       │       │ voucher_templates  │
├─────────────────────┤       ├─────────────────────┤
│ id (PK)            │       │ id (PK)            │
│ voucher_no         │       │ code               │
│ voucher_date       │       │ name               │
│ status             │       │ category           │
│ maker_id           │       │ description        │
│ checker_id         │       │ items (JSON)       │
│ poster_id          │       │ is_active          │
└────────┬────────────┘       └─────────────────────┘
         │
         │ 1:N
         ▼
┌─────────────────────┐
│   voucher_items    │
├─────────────────────┤
│ id (PK)            │
│ voucher_id (FK)    │
│ account_code       │
│ account_name       │
│ direction          │
│ amount             │
│ description        │
└─────────────────────┘


┌─────────────────────┐       ┌─────────────────────┐
│      invoices      │       │    tax_records     │
├─────────────────────┤       ├─────────────────────┤
│ id (PK)            │       │ id (PK)            │
│ invoice_no         │       │ type               │
│ type               │       │ period             │
│ amount             │       │ period_start       │
│ tax_rate           │       │ period_end         │
│ seller_name        │       │ sales_amount       │
│ buyer_name         │       │ sales_tax          │
│ date               │       │ purchase_amount    │
│ status            ─┼─────▶ │ purchase_tax       │
│ voucher_id (FK)    │       │ tax_due            │
│ created_at         │       │ status             │
└─────────────────────┘       └─────────────────────┘
```

---

## 5. 索引设计

### 5.1 公共数据库索引

| 表名 | 索引名 | 字段 | 类型 |
|------|--------|------|------|
| users | idx_users_username | username | UNIQUE |
| users | idx_users_tenant_id | tenant_id | NORMAL |
| users | idx_users_email | email | UNIQUE |
| roles | idx_roles_code | code | UNIQUE |
| user_roles | idx_user_roles_user_role | user_id, role_id | UNIQUE |
| tenants | idx_tenants_code | code | UNIQUE |
| audit_log | idx_audit_log_created | created_at | NORMAL |

### 5.2 账套数据库索引

| 表名 | 索引名 | 字段 | 类型 |
|------|--------|------|------|
| accounts | idx_accounts_code | code | UNIQUE |
| accounts | idx_accounts_parent | parent_id | NORMAL |
| vouchers | idx_vouchers_no | voucher_no | UNIQUE |
| vouchers | idx_vouchers_date | voucher_date | NORMAL |
| vouchers | idx_vouchers_status | status | NORMAL |
| voucher_items | idx_voucher_items_voucher | voucher_id | NORMAL |
| invoices | idx_invoices_no_amount | invoice_no, amount | UNIQUE |
| invoices | idx_invoices_date | date | NORMAL |
| cash_flows | idx_cash_flows_date | date | NORMAL |

---

## 6. 数据完整性设计

### 6.1 约束

| 表 | 约束类型 | 说明 |
|----|---------|------|
| users.username | UNIQUE | 用户名唯一 |
| users.email | UNIQUE | 邮箱唯一 |
| roles.code | UNIQUE | 角色编码唯一 |
| tenants.code | UNIQUE | 账套编码唯一 |
| accounts.code | UNIQUE | 科目编码唯一 |
| vouchers.voucher_no | UNIQUE | 凭证号唯一 |
| invoices | UNIQUE | 发票号+金额唯一（排除删除） |

### 6.2 外键级联

| 父表 | 子表 | 级联操作 |
|------|------|----------|
| users | user_roles | ON DELETE CASCADE |
| roles | user_roles | ON DELETE CASCADE |
| bank_accounts | cash_flows | ON DELETE RESTRICT |
| vouchers | voucher_items | ON DELETE CASCADE |
| vouchers | cash_flows | ON DELETE SET NULL |

---

## 7. 备份策略

### 7.1 备份内容

| 数据库 | 内容 | 备份频率 |
|--------|------|----------|
| finance_pub | 公共表（用户、角色、账套等） | 每日 |
| finance_xxx | 各账套数据库 | 每日 |

### 7.2 保留策略

- 最近 30 天的每日备份
- 每月末备份保留 12 个月
- 备份文件支持压缩和加密

---

*文档版本: v1.0*
*最后更新: 2026-03-27*